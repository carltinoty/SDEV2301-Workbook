Console.WriteLine("Hello to SDEV 2301 World that will run as a file based app using the command");
Console.WriteLine("dotnet Hello.cs");

//code (filename.cs) can be run directly using the command line without needing to compile it first.
//dotnet Hello.cs to activate the file based app
//ctrl + ` to open the terminal in VS Code

int count = 5;
count = 10;
count = "ten";  // This line will throw "The build failed. Fix the build errors and run again."
                //in C# it is static if its and int it stays as an int, if its a string it stays as a string, if its a double it stays as a double, etc.


